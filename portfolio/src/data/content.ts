/** @deprecated Prefer `useContent()` from `../i18n`. Kept for type re-exports. */
export type {
  Capability,
  CaseSection,
  FeaturedProject,
  Method,
  OtherProject,
  SecondaryProject,
} from '../i18n/types'
